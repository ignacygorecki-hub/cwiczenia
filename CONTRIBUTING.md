Thank you for contributing! 🎉

Please follow these simple guidelines to make collaboration smooth:

- Fork the repo and create a feature branch from `main`.
- Run the test suite and linters before opening a PR.
  - Create and activate a virtual environment: `python -m venv .venv` → `.\.venv\Scripts\Activate.ps1`
  - Install dev dependencies: `pip install -r requirements-dev.txt`
  - Run pre-commit checks: `pre-commit run --all-files`
  - Run tests: `pytest --cov=src`
- Open a PR with a clear title and description; link related issues when relevant.
- We use the **MIT** license; by contributing you agree to license your contributions under the project license.

Thanks for helping improve this project! 💡
